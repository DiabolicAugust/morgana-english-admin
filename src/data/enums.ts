export enum Fields {
  wordOfDay = 'wordOfDay',
  translation = 'translation',
  history = 'history',
  fact = 'fact',
  used = 'used',
  sentence = 'sentence',
  answer = 'answer',
  options = 'options',
  explanation = 'explanation',
}

export enum Models {
  DayWord = 'Day Word',
  Sentence = 'Sentence',
}
